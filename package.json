{
  "name": "hospital-llm-presentation",
  "version": "1.0.0",
  "description": "Presentation about local LLM usage in hospitals",
  "scripts": {
    "start": "marp -s .",
    "build": "marp presentation.md -o presentation.pdf",
    "build:pptx": "marp --allow-local-files --pptx --html presentation.md -o presentation.pptx"
  },
  "dependencies": {
    "@marp-team/marp-cli": "^2.5.0"
  }
}
